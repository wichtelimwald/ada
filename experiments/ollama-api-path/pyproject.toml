[project]
name = "ada-ollama-api-path-probe"
version = "0.0.0"
description = "Disposable Ada experiment comparing Ollama native vs OpenAI-compatible API paths"
requires-python = ">=3.11"

[tool.uv]
package = false
